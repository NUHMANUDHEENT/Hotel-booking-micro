syntax = "proto3";

package  booking_service;

option go_package = "booking_service/proto;proto";

service BookingService {
    rpc RoomBooking(RoomBookingRequest) returns (RoomBookingResponse);
}

message RoomBookingRequest{
    string room_number =1;
}
message RoomBookingResponse{
    string payment_id = 1;
}